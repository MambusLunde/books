language: python
addons:
  chrome: stable
  apt:
    packages:
      - chromium-chromedriver

install:
  - pip install -e .
  - pip install codecov pytest-cov selenium bs4 python-levenshtein
script:
  - pytest --cov=src tests
after_success:
  - codecov
